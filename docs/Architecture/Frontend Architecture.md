# Frontend Architecture

React 18 + TypeScript SPA built with Vite, using shadcn/ui for components.

## Directory Structure

```
frontend/src/
├── App.tsx                 # Root component, [[Provider Stack]]
├── main.tsx                # Vite entry point
├── index.css               # Global styles + CSS variables
├── pages/                  # Route-level components
│   ├── HomePage.tsx
│   ├── LoginPage.tsx
│   ├── ProfilePage.tsx
│   └── RegisterPage.tsx
├── components/
│   ├── ui/                 # shadcn/ui primitives (see [[Component Library]])
│   ├── auth/               # AuthCard, GoogleLoginButton
│   └── common/             # AppSidebar
├── providers/
│   ├── AuthProvider.tsx    # Session validation on mount (see [[Auth Flow]])
│   ├── RouteGuard.tsx      # Redirect unauthenticated users
│   └── SnackbarProvider.tsx
├── router/
│   └── router.tsx          # React Router route definitions
├── store/                  # [[State Management|Redux Toolkit]]
│   ├── store.ts
│   ├── hooks.ts            # Typed useAppDispatch, useAppSelector
│   └── components/
│       ├── authSlice.ts    # User state + localStorage
│       └── snackbarSlice.ts
├── openapi/                # [[OpenAPI Type Generation|Auto-generated]]
│   └── sieveBase.ts        # API client + React Query hooks
├── hooks/                  # Custom React hooks
└── lib/                    # Utilities (cn(), etc.)
```

## Key Conventions

- **Path alias**: `@/` maps to `src/` - never use relative `../` imports. Barrel exports may use `./`
- **Pages vs Components**: Pages are route-level, components are reusable
- **Auto-generated API**: `src/openapi/` is generated by Orval - never edit manually
- **CSS**: Tailwind via `cn()` utility for conditional classes, CSS variables for theming (see [[shadcn-ui Guide]])
- **Barrel exports**: Use `index.ts` in component directories for clean imports
- **Environment vars**: `window.env.*` for runtime config, `import.meta.env.VITE_*` for build-time (see [[Environment Setup]])

## Routes

| Path | Page | Auth Required |
|------|------|--------------|
| `/` | Redirect to `/home` | Yes |
| `/home` | HomePage | Yes |
| `/login` | LoginPage | No |
| `/profile` | ProfilePage | Yes |
| `/register` | RegisterPage | No |

## Build & Dev

- **Dev server**: `yarn start` (Vite, port 5173 standalone or 8080 in Docker)
- **Production**: Multi-stage Docker build → Nginx serving static files
- **Type generation**: `make sync_openapi` regenerates `openapi/sieveBase.ts`

## See Also

- [[Provider Stack]]
- [[State Management]]
- [[Component Library]]
