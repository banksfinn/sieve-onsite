notes:
- id: agents
  title: AI Agent Guidelines
  purpose: 'Core guidelines for AI agents working in this codebase.

    These rules apply to all AI interactions with the repository.

    '
  paths:
  - '**/*'
  tags:
  - ai
  - agents
  - general
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: Before working on any file, call `get_notes_for_path("path/to/file")`
      to retrieve relevant guideline...
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: When you discover important patterns, gotchas, or conventions during
      work, document them by calling ...
  - index: 2
    source: user
    status: active
    enforcement: strict
    preview: 'Before editing user-created items (`source: user`), ask the user for
      approval. LLM-created items can...'
  - index: 3
    source: user
    status: active
    enforcement: strict
    preview: Always follow the patterns established in CLAUDE.md for this project.
  - index: 4
    source: user
    status: active
    enforcement: locked
    preview: Never commit secrets, API keys, or credentials to the repository.
  - index: 5
    source: user
    status: active
    enforcement: recommended
    preview: When creating new files, follow the existing directory structure and
      naming conventions.
  - index: 6
    source: user
    status: active
    enforcement: recommended
    preview: Run tests and linting before suggesting code is complete.
- id: api-patterns
  title: API & OpenAPI Patterns
  purpose: FastAPI route patterns, OpenAPI generation, and frontend type synchronization.
  paths:
  - backend/app/api/**
  - frontend/src/openapi/**
  tags:
  - api
  - openapi
  - routes
  - fastapi
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: Use Pydantic models for all API request/response schemas. This ensures
      automatic OpenAPI documentati...
  - index: 1
    source: user
    status: active
    enforcement: locked
    preview: Never use `dict` as an API response type. Always define a Pydantic model,
      even for simple responses.
  - index: 2
    source: user
    status: active
    enforcement: strict
    preview: After adding or modifying API endpoints, run `make sync_openapi` to regenerate
      frontend types.
  - index: 3
    source: user
    status: active
    enforcement: locked
    preview: Files in `frontend/src/openapi/` are auto-generated by Orval. Do not
      edit manually.
  - index: 4
    source: user
    status: active
    enforcement: strict
    preview: Register new route modules in `app/api/router.py`.
  - index: 5
    source: user
    status: active
    enforcement: strict
    preview: '**Async route handlers**: Always use `async def` for FastAPI route handlers.
      This ensures consistent...'
- id: authentication
  title: Authentication
  purpose: Cookie-based authentication flow, UserDependency, and protected routes.
  paths:
  - backend/libs/user_management/**
  - backend/app/api/routes/auth*
  tags:
  - auth
  - security
  - cookies
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: '**Cookie-based auth**: Uses first-party cookies. On login/register,
      backend encrypts user ID into a ...'
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: '**UserDependency**: Protected routes use `UserDependency` in FastAPI
      route parameters (defined in `b...'
  - index: 2
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Frontend AuthProvider** (`frontend/src/providers/AuthProvider.tsx`):
      Top-level provider with no ro...'
  - index: 3
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Route Configuration** (`frontend/src/router/routes.ts`): Defines all
      page routes centrally. `PUBLI...'
  - index: 4
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Auth State** (`frontend/src/store/components/authSlice.ts`): Redux
      slice storing `user` (User obje...'
  - index: 5
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Refresh Endpoint** (`POST /api/gateway/user/refresh`): Backend endpoint
      that validates the existin...'
  - index: 6
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Login Flow**: GoogleLoginButton calls `/api/gateway/authentication/google/login`
      with ID token, th...'
  - index: 7
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**RouteGuard** (`frontend/src/providers/RouteGuard.tsx`): Must be inside
      BrowserRouter. Handles redi...'
- id: backend-structure
  title: Backend Structure & Patterns
  purpose: Architecture and patterns for the FastAPI backend including directory structure,
    entry points, and code organization.
  paths:
  - backend/**
  tags:
  - backend
  - python
  - fastapi
  - architecture
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: 'The backend follows a two-tier architecture: `app/` contains production
      runtime code, `libs/` contai...'
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: Place route handlers in `app/api/routes/`.
  - index: 2
    source: user
    status: active
    enforcement: recommended
    preview: Place Celery task definitions in `app/tasks/`.
  - index: 3
    source: user
    status: active
    enforcement: strict
    preview: Entity definitions (SQLAlchemy models + Pydantic schemas) belong in `app/blueprints/`.
      Each entity g...
  - index: 4
    source: user
    status: active
    enforcement: strict
    preview: Data access layer implementations (BaseEntityStore subclasses) belong
      in `app/stores/`.
  - index: 5
    source: user
    status: active
    enforcement: recommended
    preview: 'Entry points: `app/gateway.py` (FastAPI app), `app/worker.py` (Celery
      app), `app/config.py` (configu...'
  - index: 6
    source: user
    status: active
    enforcement: recommended
    preview: 'Reusable libraries in `libs/` include: `database_manager/` (SQLAlchemy
      abstraction), `user_managemen...'
  - index: 7
    source: user
    status: active
    enforcement: strict
    preview: The Gateway (`app/gateway.py`) should be kept lightweight, containing
      only truly global configuratio...
  - index: 8
    source: user
    status: active
    enforcement: strict
    preview: '`__init__.py` files must always be empty. Only place them at the root
      of a package (e.g., `locking_m...'
  - index: 9
    source: user
    status: active
    enforcement: strict
    preview: The `tools/` directory contains development tooling (not deployed). Single-file
      scripts are fine as ...
- id: component-library
  title: Shared Component Library
  purpose: Documents all reusable UI components available in the frontend. Check this
    before creating new components to avoid duplication.
  paths:
  - frontend/src/**
  tags:
  - frontend
  - components
  - shadcn
  - ui
  items:
  - index: 0
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Button** (`@/components/ui/button`) - Standard button with variants:
      `default`, `destructive`, `ou...'
  - index: 1
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Card** (`@/components/ui/card`) - Container component with sub-components:
      `Card`, `CardHeader`, `...'
  - index: 2
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**AuthCard** (`@/components/auth`) - Centered card layout for authentication
      pages. Uses composition...'
  - index: 3
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**GoogleLoginButton** (`@/components/auth`) - Google OAuth login button.
      Props: `onSuccess`, `onErro...'
  - index: 4
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**AppSidebar** (`@/components/common`) - Application sidebar layout
      with navigation. Props: `navGrou...'
  - index: 5
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Snackbar** (`@/store/components/snackbarSlice`, `@/providers/SnackbarProvider`)
      - Toast notificati...'
  - index: 6
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**TagChip** (`@/components/tags`) - Compact inline tag display with
      colored dot. Props: `tag` (TodoT...'
  - index: 7
    source: user
    status: active
    enforcement: strict
    preview: '**Before creating components**: 1) Can shadcn do this? 2) Can I compose
      existing components? 3) Will...'
- id: database-patterns
  title: Database Patterns
  purpose: SQLAlchemy models, Pydantic schemas, BaseEntityStore pattern, and Alembic
    migrations.
  paths:
  - backend/app/blueprints/**
  - backend/app/stores/**
  - backend/tools/alembic/**
  tags:
  - database
  - sqlalchemy
  - pydantic
  - migrations
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: 'Blueprint files define four components: `FooModel` (SQLAlchemy model),
      `Foo` (Pydantic entity), `Foo...'
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: Data access stores inherit from `BaseEntityStore` with generic type parameters
      for the model and ent...
  - index: 2
    source: user
    status: active
    enforcement: strict
    preview: Alembic `env.py` must import all models for migration discovery. When
      adding new models, ensure they...
  - index: 3
    source: user
    status: active
    enforcement: recommended
    preview: 'Terminology: "Model" refers to SQLAlchemy database models (in `blueprints/`).
      "Entity" refers to Pyd...'
  - index: 4
    source: user
    status: active
    enforcement: strict
    preview: Use `model_config = ConfigDict(from_attributes=True)` for Pydantic models
      that map from SQLAlchemy (...
  - index: 5
    source: user
    status: active
    enforcement: strict
    preview: '**Creating a new entity**: 1) Define blueprints (FooModel, Foo, FooCreateRequest,
      FooUpdateRequest),...'
  - index: 6
    source: user
    status: active
    enforcement: strict
    preview: '**Custom queries**: Override `_apply_entity_specific_search(query, search)`
      in your store to add ent...'
  - index: 7
    source: user
    status: active
    enforcement: strict
    preview: '**ForeignKey relationships**: Define with `mapped_column(ForeignKey(table
      + ".id", ondelete="CASCADE...'
  - index: 8
    source: user
    status: active
    enforcement: strict
    preview: '**Eager loading joins**: Add related models to `selectinload_fields`
      in the store. Override `to_dict...'
  - index: 9
    source: user
    status: active
    enforcement: strict
    preview: '**Alembic migrations**: Alembic (`tools/alembic/`) manages database
      migrations. Use `make db_generat...'
  - index: 10
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Alembic workflow**: After creating a migration with `make db_generate_migration`,
      always: 1) Revie...'
  - index: 11
    source: llm
    status: proposed
    enforcement: strict
    preview: '**Async database access**: All database operations use async SQLAlchemy
      with `asyncpg`. Store method...'
  - index: 12
    source: llm
    status: proposed
    enforcement: recommended
    preview: '**Celery async integration**: The `@locked_task` decorator now expects
      async functions. Write Celery...'
  - index: 13
    source: llm
    status: proposed
    enforcement: strict
    preview: '**Query syntax change**: Entity-specific search filters now use SQLAlchemy
      2.0 `Select` statements i...'
  - index: 14
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Async lazy loading gotcha**: In `create_entity`, after `flush()` the
      new model has no loaded relat...'
- id: docker-infrastructure
  title: Docker & Infrastructure
  purpose: Docker services, compose configuration, and local development setup.
  paths:
  - infra/**/*
  - '**/Dockerfile'
  - '**/docker-compose*.yml'
  tags:
  - docker
  - infrastructure
  - devops
  items:
  - index: 0
    source: user
    status: active
    enforcement: recommended
    preview: 'Local development uses `infra/docker/local/docker-compose.yml`. Port
      allocation (1300-1310 range): F...'
  - index: 1
    source: user
    status: active
    enforcement: recommended
    preview: Use `make run` for full stack, `make run_no_frontend` for gateway + database
      only (faster frontend h...
  - index: 2
    source: llm
    status: proposed
    enforcement: flexible
    preview: Celery worker and beat run as non-root user (1000:1000) to avoid security
      warnings. Redis is used as...
- id: environment-setup
  title: Development Environment
  purpose: 'Expected local machine state and environment configuration

    required for development.

    '
  paths:
  - '**/*'
  tags:
  - environment
  - setup
  - node
  - python
  - bitwarden
  - secrets
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: Node.js 22.x is required. The MCP server runs commands via `bash -l`,
      so nvm's default must be set c...
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: 'Python 3.12 is required. Use pyenv to manage versions: `pyenv shell
      3.12.2`.'
  - index: 2
    source: user
    status: active
    enforcement: recommended
    preview: Use `uv` for Python package management, not pip directly.
  - index: 3
    source: user
    status: active
    enforcement: recommended
    preview: If MCP tools fail with Node version errors, check that `bash -l -c "node
      --version"` returns the exp...
  - index: 4
    source: user
    status: active
    enforcement: strict
    preview: Bitwarden CLI is required for secrets management. Install with `brew
      install bitwarden-cli` and logi...
  - index: 5
    source: user
    status: active
    enforcement: strict
    preview: '`.env` files are generated from `.env.example` using `scripts/administration/env/expand_env.sh`.
      Val...'
  - index: 6
    source: user
    status: active
    enforcement: strict
    preview: Bitwarden secrets are stored in the "Lavender" folder (configured in
      `expand_env.sh`). The vault mus...
- id: frontend-patterns
  title: Frontend Patterns
  purpose: React + TypeScript patterns including component organization, styling with
    shadcn/ui, and state management.
  paths:
  - frontend/src/**
  tags:
  - frontend
  - react
  - typescript
  - shadcn
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: Use the `@/` path alias for all imports (configured in `tsconfig.json`
      and `vite.config.ts`). Exampl...
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: Place page components in `pages/` directory. Place reusable components
      in `components/`.
  - index: 2
    source: user
    status: active
    enforcement: recommended
    preview: Custom hooks go in `hooks/`. React context providers go in `providers/`.
      Router configuration is in ...
  - index: 3
    source: user
    status: active
    enforcement: recommended
    preview: State management uses Redux Toolkit in `store/`.
  - index: 4
    source: user
    status: active
    enforcement: recommended
    preview: Theming uses CSS variables in `src/index.css`. Light/dark mode is supported.
  - index: 5
    source: user
    status: active
    enforcement: strict
    preview: '**Tech stack**: React 18 + TypeScript, Vite build tooling, shadcn/ui,
      React Query (TanStack Query) f...'
  - index: 6
    source: user
    status: active
    enforcement: strict
    preview: '**Server state (React Query)**: Use generated Orval hooks from `@/openapi/fullstackBase`
      for API cal...'
  - index: 7
    source: user
    status: active
    enforcement: strict
    preview: '**Client state (Redux)**: Use typed hooks from `@/store/hooks`. Example:
      `const dispatch = useAppDis...'
  - index: 8
    source: user
    status: active
    enforcement: strict
    preview: '**Environment variables**: Access via `window.ENV` (injected at runtime).
      For build-time, use `impor...'
  - index: 9
    source: user
    status: active
    enforcement: strict
    preview: '**Component structure**: Define Props interface with JSDoc comments,
      include `className?` for stylin...'
  - index: 10
    source: user
    status: active
    enforcement: strict
    preview: '**Barrel exports**: Each component directory should have `index.ts`
      for clean imports. Example: `exp...'
  - index: 11
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**API error handling pattern**: Always provide user feedback for backend
      requests. Use snackbars for...'
  - index: 12
    source: llm
    status: proposed
    enforcement: flexible
    preview: '**Import paths**: Always use absolute imports with `@/` prefix. The
      only exception is barrel export ...'
- id: mcp-server
  title: MCP Dev Server
  purpose: 'Guidelines for the FastMCP development server that provides

    AI-accessible tools for common development tasks.

    '
  paths:
  - backend/tools/mcp_server/**/*
  tags:
  - mcp
  - dev-tools
  - ai
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: The MCP server runs automatically on startup via VSCode tasks (`.vscode/tasks.json`).
      No manual star...
  - index: 1
    source: user
    status: active
    enforcement: recommended
    preview: Use Pydantic models for tool return types to maintain type safety.
  - index: 2
    source: user
    status: active
    enforcement: recommended
    preview: 'Use explicit imports from submodules: `from mcp_server.notes.models
      import Note` rather than re-expo...'
  - index: 3
    source: user
    status: active
    enforcement: strict
    preview: The MCP server is structured as a proper Python package at `tools/mcp_server/`
      with its own `pyproje...
- id: shadcn-ui
  title: shadcn/ui Component Guidelines
  purpose: Guidelines for using and extending shadcn/ui components, theming, and form
    handling.
  paths:
  - frontend/src/**
  tags:
  - frontend
  - shadcn
  - ui
  - components
  - tailwind
  items:
  - index: 0
    source: user
    status: active
    enforcement: strict
    preview: '**Adding components**: Run `npx shadcn@latest add <component>` from
      the `frontend/` directory. Compo...'
  - index: 1
    source: user
    status: active
    enforcement: strict
    preview: '**cn() utility**: Use `cn()` from `@/lib/utils` to merge Tailwind classes
      conditionally. Example: `c...'
  - index: 2
    source: user
    status: active
    enforcement: strict
    preview: '**Theming**: Uses CSS variables in `src/index.css`. Semantic tokens:
      `background/foreground`, `prima...'
  - index: 3
    source: user
    status: active
    enforcement: strict
    preview: '**Component composition**: Use sub-components for complex UI. Example:
      `<Card><CardHeader><CardTitle...'
  - index: 4
    source: user
    status: active
    enforcement: strict
    preview: '**Form handling**: Use shadcn/ui form components with react-hook-form
      and zod. Add via `npx shadcn@l...'
  - index: 5
    source: user
    status: active
    enforcement: strict
    preview: '**Icons**: Use Lucide React for icons (already installed). Example:
      `import { Search, Menu } from ''l...'
  - index: 6
    source: user
    status: active
    enforcement: strict
    preview: '**Dark mode**: Supported via `dark` class on root element. CSS variables
      automatically switch. Toggl...'
